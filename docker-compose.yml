version: '3.9'
services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: fitmorph
    ports:
      - "5432:5432"
    volumes:
      - fitmorph_pg:/var/lib/postgresql/data
volumes:
  fitmorph_pg:
